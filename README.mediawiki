Position, scale and rotation seem to work as expected, though they have all only had somewhat limited testing.

'''''A VSfilter compatibility mode has been implemented for allowing VSfilter to work with scale data. It helps to stabilize the way vsfilter renders non-integer scale values, but libASS's rendering job is still superior. Also, as it uses a lot of staged transforms, I have no idea how much it increases the rendering cost. I'm guessing a fair amount.'''''

This script is a horrible amalgam of duct tape, ugly string matching and generally all around terrible code. For more information on how to use it, see the user manual.<br>If you have any questions, feel free to contact me on rizon.

This project was previously known as aegisub-mocha.